# cricket-match-analyzer-pro
A Python-based Cricket Match Analyzer that takes ball-by-ball score input, calculates total runs, boundaries, dot balls, and strike rate. Features robust input validation (Exception Handling) to prevent crashes from invalid data.
